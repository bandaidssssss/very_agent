{
  "data": {
    "data.train_batch_size": {"stage": "hardware", "type": "int", "impact": "Number of prompts in one training update; changes total work and data-parallel scheduling more than single-kernel peak memory.", "increase": "More work per update and potentially better amortization, but longer steps and more rollout samples.", "decrease": "Shorter updates with noisier measurements and potentially lower utilization.", "interactions": ["actor_rollout_ref.rollout.n", "actor_rollout_ref.actor.ppo_mini_batch_size"], "constraints": ["train_batch_size * rollout.n must be divisible by ppo_mini_batch_size", "hardware comparisons preserve the total token budget"]},
    "data.max_prompt_length": {"stage": "hardware", "type": "int", "impact": "Maximum prompt tokens; affects prefill compute, activations, KV cache, and padded work.", "increase": "Raises rollout and log-prob memory/latency, especially without remove padding.", "decrease": "Lowers pressure but changes the workload unless compensated under the token-budget rule.", "interactions": ["data.max_response_length", "rollout.enable_chunked_prefill", "actor.megatron.use_remove_padding"], "constraints": ["Must match dataset truncation policy", "hardware token budget is frozen"]},
    "data.max_response_length": {"stage": "hardware", "type": "int", "impact": "Maximum generated tokens; strongly affects decode time, KV cache, log-prob, and actor-training activations.", "increase": "Raises all four phase costs and makes rollout more likely to dominate.", "decrease": "Reduces memory/time but changes the RL workload unless budget is preserved.", "interactions": ["rollout.n", "rollout.gpu_memory_utilization", "log-prob micro batches"], "constraints": ["hardware token budget is frozen"]},
    "actor_rollout_ref.actor.use_dynamic_bsz": {"stage": "hardware", "type": "bool", "impact": "Uses token-aware dynamic micro batches instead of fixed sample counts.", "increase": "Can improve packing and reduce padding variance, but changes which fixed micro-batch settings are authoritative.", "decrease": "Makes fixed per-GPU micro batches directly control work.", "interactions": ["actor.ppo_micro_batch_size_per_gpu", "actor.megatron.use_remove_padding"], "constraints": ["Verify the selected verl backend supports the dynamic-batch fields"]},
    "actor_rollout_ref.actor.ppo_micro_batch_size_per_gpu": {"stage": "hardware", "type": "int", "impact": "Per-GPU PPO update micro batch; a primary training activation-memory and kernel-utilization control.", "increase": "Usually improves training utilization but raises training peak memory.", "decrease": "Lowers training memory with more gradient-accumulation iterations.", "interactions": ["actor.ppo_mini_batch_size", "actor TP/PP", "recompute"], "constraints": ["ppo_mini_batch_size must be divisible by this value"]},
    "actor_rollout_ref.actor.ppo_mini_batch_size": {"stage": "hardware", "type": "int", "impact": "PPO mini-batch size per update; controls optimization grouping and the number of PPO mini batches.", "increase": "Changes optimization granularity and may reduce loop overhead, but is not the main per-kernel memory control.", "decrease": "Creates more mini-batch partitions and may increase overhead.", "interactions": ["data.train_batch_size", "rollout.n", "actor.ppo_micro_batch_size_per_gpu"], "constraints": ["train_batch_size * rollout.n must be divisible by it", "it must be divisible by ppo_micro_batch_size_per_gpu"]},
    "actor_rollout_ref.actor.megatron.tensor_model_parallel_size": {"stage": "hardware", "type": "int", "impact": "Shards actor tensor computation and model states across ranks.", "increase": "Lowers per-rank model/activation pressure but adds tensor-parallel communication.", "decrease": "Reduces communication and can improve kernels if memory remains feasible.", "interactions": ["actor PP", "actor sequence_parallel", "world size"], "constraints": ["world size must be divisible by actor TP * PP"]},
    "actor_rollout_ref.actor.megatron.pipeline_model_parallel_size": {"stage": "hardware", "type": "int", "impact": "Partitions actor layers into pipeline stages.", "increase": "Reduces layers per rank but adds bubbles, transfers, and scheduling complexity.", "decrease": "Reduces pipeline overhead but increases per-rank model memory.", "interactions": ["actor TP", "micro batch", "recompute"], "constraints": ["world size must be divisible by actor TP * PP"]},
    "actor_rollout_ref.actor.megatron.sequence_parallel": {"stage": "hardware", "type": "bool", "impact": "Shards eligible sequence-dimension activations within tensor-parallel groups.", "increase": "Usually lowers actor activation memory with extra collective communication.", "decrease": "Avoids those collectives but raises activation replication.", "interactions": ["actor TP", "long sequences"], "constraints": ["Most useful when TP > 1 and backend kernels support it"]},
    "actor_rollout_ref.actor.megatron.use_distributed_optimizer": {"stage": "hardware", "type": "bool", "impact": "Shards optimizer states and related buffers across data-parallel ranks.", "increase": "Reduces training optimizer memory with communication/bookkeeping cost.", "decrease": "Replicates more optimizer state and may simplify execution.", "interactions": ["optimizer_offload", "data parallel size"], "constraints": ["Requires compatible Megatron/distributed backend"]},
    "actor_rollout_ref.actor.megatron.use_remove_padding": {"stage": "hardware", "type": "bool", "impact": "Removes padded tokens from actor computation when supported.", "increase": "Reduces wasted compute and memory for variable-length samples.", "decrease": "Uses regular padded shapes, which may be simpler but wasteful.", "interactions": ["max prompt/response length", "dynamic batch"], "constraints": ["Validate kernel/backend compatibility"]},
    "actor_rollout_ref.actor.megatron.optimizer_offload": {"stage": "hardware", "type": "bool", "impact": "Moves actor optimizer states away from GPU between/within training operations.", "increase": "Lowers training GPU memory but adds host/device transfer latency.", "decrease": "Keeps optimizer state on GPU for speed at higher memory cost.", "interactions": ["distributed optimizer", "host bandwidth", "param_offload"], "constraints": ["Host memory must be sufficient"]},
    "actor_rollout_ref.actor.megatron.param_offload": {"stage": "hardware", "type": "bool", "impact": "Moves actor parameters away from GPU when inactive.", "increase": "Creates headroom for rollout/ref phases but adds reload transfer time.", "decrease": "Keeps parameters resident for faster phase transitions at higher memory use.", "interactions": ["free_cache_engine", "optimizer_offload", "hybrid engine"], "constraints": ["Host memory and transfer bandwidth must be sufficient"]},
    "actor_rollout_ref.actor.megatron.override_transformer_config.recompute_granularity": {"stage": "hardware", "type": "string/null", "impact": "Selects activation recomputation scope, commonly full or selective.", "increase": "Stronger recomputation lowers training activation memory and increases compute.", "decrease": "Stores more activations for faster backward at higher peak memory.", "interactions": ["recompute_method", "recompute_num_layers", "recompute_modules"], "constraints": ["Values must be supported by the installed Megatron version"]},
    "actor_rollout_ref.actor.megatron.override_transformer_config.recompute_modules": {"stage": "hardware", "type": "list/string", "impact": "Selects transformer submodules included in selective recomputation.", "increase": "More modules reduce saved activations but add repeated forward work.", "decrease": "Less repeated compute but higher activation memory.", "interactions": ["recompute_granularity"], "constraints": ["Module names are backend-version specific; search local verl/Megatron docs first"]},
    "actor_rollout_ref.actor.megatron.override_transformer_config.recompute_method": {"stage": "hardware", "type": "string/null", "impact": "Controls how recomputation is distributed across transformer layers.", "increase": "Method choice shifts the memory/compute pattern across layers.", "decrease": "No universal monotonic direction; compare supported uniform/block behavior.", "interactions": ["recompute_num_layers", "recompute_granularity"], "constraints": ["Must match backend-supported enum values"]},
    "actor_rollout_ref.actor.megatron.override_transformer_config.recompute_num_layers": {"stage": "hardware", "type": "int/null", "impact": "Number or grouping of layers recomputed under the selected method.", "increase": "Usually lowers stored activation memory and raises repeated compute.", "decrease": "Usually improves speed while consuming more activation memory.", "interactions": ["recompute_method", "pipeline parallelism"], "constraints": ["Meaning depends on recompute_method and Megatron version"]},
    "actor_rollout_ref.rollout.tensor_model_parallel_size": {"stage": "hardware", "type": "int", "impact": "Shards rollout model execution and related cache/model state across ranks.", "increase": "Lowers per-rank weight pressure but adds communication and can reduce vLLM scheduling efficiency.", "decrease": "Reduces communication and may improve decode throughput if memory fits.", "interactions": ["gpu_memory_utilization", "max_num_batched_tokens", "world size"], "constraints": ["world size must be divisible by rollout TP"]},
    "actor_rollout_ref.rollout.log_prob_micro_batch_size_per_gpu": {"stage": "hardware", "type": "int", "impact": "Per-GPU micro batch for actor old-log-prob computation.", "increase": "Improves actor_log_prob utilization but raises that phase's activation peak.", "decrease": "Lowers actor_log_prob memory with more iterations.", "interactions": ["actor TP", "sequence lengths", "remove padding"], "constraints": ["Positive integer; validate backend batching semantics"]},
    "actor_rollout_ref.rollout.gpu_memory_utilization": {"stage": "hardware", "type": "float", "impact": "Target fraction of GPU memory available to the rollout engine, dominated by model/KV-cache reservation.", "increase": "Allows more KV cache/concurrency but can starve actor/ref/training in a colocated process.", "decrease": "Creates cross-phase headroom but may lower rollout concurrency.", "interactions": ["max_num_batched_tokens", "max_num_seqs", "free_cache_engine"], "constraints": ["Agent range 0.1-0.95", "must remain below measured phase safety line"]},
    "actor_rollout_ref.rollout.max_num_batched_tokens": {"stage": "hardware", "type": "int", "impact": "Maximum tokens the rollout scheduler batches at once.", "increase": "Can improve prefill/decode utilization but raises temporary KV/activation pressure.", "decrease": "Reduces peak scheduling pressure with more scheduling waves.", "interactions": ["max_num_seqs", "chunked prefill", "gpu_memory_utilization"], "constraints": ["Must cover viable request shapes for the configured sequence lengths"]},
    "actor_rollout_ref.rollout.max_num_seqs": {"stage": "hardware", "type": "int", "impact": "Maximum concurrent sequences in the rollout scheduler.", "increase": "Can improve batching and throughput but consumes more KV-cache metadata/state.", "decrease": "Reduces concurrency memory and may underutilize the device.", "interactions": ["max_num_batched_tokens", "rollout.n", "gpu_memory_utilization"], "constraints": ["Positive integer"]},
    "actor_rollout_ref.rollout.free_cache_engine": {"stage": "hardware", "type": "bool", "impact": "Releases rollout cache-engine memory when moving to other colocated phases.", "increase": "Creates actor/ref/training headroom but adds cache teardown/recreation overhead.", "decrease": "Improves phase transition speed while keeping more rollout memory resident.", "interactions": ["actor/ref param offload", "gpu_memory_utilization"], "constraints": ["Behavior depends on rollout backend"]},
    "actor_rollout_ref.rollout.enable_chunked_prefill": {"stage": "hardware", "type": "bool", "impact": "Splits long prompt prefills into scheduler chunks.", "increase": "Reduces single-prefill peaks and can mix prefill/decode work, with scheduling overhead.", "decrease": "Uses simpler full prefills that may peak higher for long prompts.", "interactions": ["max_num_batched_tokens", "max_prompt_length"], "constraints": ["Requires rollout-backend support"]},
    "actor_rollout_ref.rollout.enable_prefix_caching": {"stage": "hardware", "type": "bool", "impact": "Caches reusable prompt prefixes in rollout KV state.", "increase": "Speeds repeated prefixes but retains cache entries and adds bookkeeping.", "decrease": "Avoids cache retention when prompts do not share useful prefixes.", "interactions": ["gpu_memory_utilization", "dataset prompt similarity"], "constraints": ["Benefit depends on actual prefix reuse"]},
    "actor_rollout_ref.rollout.enforce_eager": {"stage": "hardware", "type": "bool", "impact": "Forces eager rollout execution instead of graph-based execution where available.", "increase": "Often improves compatibility and reduces graph-capture memory, but can lower throughput.", "decrease": "Allows graph optimizations with capture/startup memory and backend constraints.", "interactions": ["platform backend", "sequence-shape variability"], "constraints": ["Use the installed rollout backend's semantics"]},
    "actor_rollout_ref.ref.megatron.tensor_model_parallel_size": {"stage": "hardware", "type": "int", "impact": "Shards reference-model tensor computation and states.", "increase": "Lowers ref per-rank pressure but adds collectives.", "decrease": "Reduces communication if the reference model fits.", "interactions": ["ref PP", "ref sequence_parallel", "world size"], "constraints": ["world size must be divisible by ref TP * PP"]},
    "actor_rollout_ref.ref.megatron.pipeline_model_parallel_size": {"stage": "hardware", "type": "int", "impact": "Partitions reference-model layers into pipeline stages.", "increase": "Lowers per-rank model memory but adds pipeline overhead.", "decrease": "Reduces pipeline overhead at higher memory per rank.", "interactions": ["ref TP", "ref log-prob micro batch"], "constraints": ["world size must be divisible by ref TP * PP"]},
    "actor_rollout_ref.ref.megatron.sequence_parallel": {"stage": "hardware", "type": "bool", "impact": "Shards eligible reference-model sequence activations within TP groups.", "increase": "Lowers ref activation memory with collective cost.", "decrease": "Avoids sequence collectives at higher activation replication.", "interactions": ["ref TP", "sequence lengths"], "constraints": ["Most useful when TP > 1"]},
    "actor_rollout_ref.ref.megatron.param_offload": {"stage": "hardware", "type": "bool", "impact": "Offloads inactive reference-model parameters from GPU.", "increase": "Reduces cross-phase resident memory but adds reload transfers.", "decrease": "Keeps ref parameters resident for speed at higher GPU memory cost.", "interactions": ["ref TP/PP", "actor/rollout colocation"], "constraints": ["Host memory and bandwidth must be sufficient"]},
    "actor_rollout_ref.ref.log_prob_micro_batch_size_per_gpu": {"stage": "hardware", "type": "int", "impact": "Per-GPU micro batch for reference log-prob forward passes.", "increase": "Improves ref utilization but raises ref_log_prob peak memory.", "decrease": "Lowers ref memory with more forward iterations.", "interactions": ["ref TP/PP", "sequence length", "ref param offload"], "constraints": ["Positive integer"]},
    "actor_rollout_ref.actor.optim.lr": {"stage": "stability", "type": "float", "impact": "Actor optimizer learning rate; a primary RL update-size and stability control.", "increase": "Can learn faster but raises KL/clip pressure and collapse risk.", "decrease": "Usually improves stability but may slow reward improvement.", "interactions": ["lr_warmup_steps", "kl_loss_coef", "ppo mini batch"], "constraints": ["Agent range 1e-7 to 1e-5"]},
    "actor_rollout_ref.actor.optim.lr_warmup_steps": {"stage": "stability", "type": "int", "impact": "Number of initial optimizer steps used to ramp the learning rate.", "increase": "Reduces early shocks but delays full learning rate.", "decrease": "Reaches target LR sooner with more startup instability risk.", "interactions": ["actor optim.lr", "trial update budget"], "constraints": ["Warmup must be meaningfully smaller than the evaluation horizon"]},
    "actor_rollout_ref.actor.use_kl_loss": {"stage": "stability", "type": "bool", "impact": "Enables an explicit actor KL penalty in the optimization loss.", "increase": "Constrains policy drift when coefficient/type are configured.", "decrease": "Removes this loss constraint; reward-side KL is a separate mechanism.", "interactions": ["kl_loss_coef", "kl_loss_type", "algorithm.use_kl_in_reward"], "constraints": ["Do not confuse loss KL with reward KL"]},
    "actor_rollout_ref.actor.kl_loss_coef": {"stage": "stability", "type": "float", "impact": "Weight of the actor KL loss.", "increase": "Restrains policy drift but can suppress reward learning.", "decrease": "Allows larger policy updates with greater KL risk.", "interactions": ["use_kl_loss", "learning rate", "kl_loss_type"], "constraints": ["Has effect only when use_kl_loss is true", "Agent range 0-0.2"]},
    "actor_rollout_ref.actor.kl_loss_type": {"stage": "stability", "type": "string", "impact": "Estimator/formulation used for KL loss; changes numerical behavior and variance.", "increase": "No monotonic direction; select based on installed verl semantics and observed KL behavior.", "decrease": "No monotonic direction.", "interactions": ["kl_loss_coef", "use_kl_loss"], "constraints": ["Search local verl source for supported enum values before changing"]},
    "actor_rollout_ref.actor.entropy_coeff": {"stage": "stability", "type": "float", "impact": "Weight of entropy regularization in the actor objective.", "increase": "Encourages exploration but may weaken task-reward optimization.", "decrease": "Makes updates more reward-directed and may reduce diversity.", "interactions": ["reward", "actor entropy", "learning rate"], "constraints": ["Agent range 0-0.2; diagnose entropy behavior before changing"]},
    "actor_rollout_ref.rollout.n": {"stage": "stability", "type": "int", "impact": "Number of responses sampled per prompt for GRPO grouping.", "increase": "Provides more within-prompt comparisons but increases rollout/token work and can affect variance.", "decrease": "Reduces rollout cost but weakens group statistics.", "interactions": ["train_batch_size", "ppo_mini_batch_size", "hardware token budget"], "constraints": ["train_batch_size * n must be divisible by ppo_mini_batch_size", "Changing n changes workload and requires careful end-to-end comparison"]}
  }
}
